{
  "name": "misterdonut",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "nohoist": [
      "**"
    ]
  },
  "scripts": {
    "build": "turbo run build",
    "predev": "npm run gen",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write \"**/*.{js,ts,tsx,md,graphql,json,yaml,yml}\" && sort-package-json 'package.json' 'packages/*/package.json' 'apps/*/package.json'",
    "gen": "npm run gen -w @misterdonut/graphql-codegen",
    "graph": "turbo run build --graph=assets/dependencies-graph.png",
    "lint": "turbo run lint",
    "prepare": "husky install"
  },
  "resolutions": {
    "@types/react": "18.0.15",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/types": "^17.0.0",
    "commitlint-config-gitmoji": "^2.2.5",
    "eslint-config-custom": "*",
    "husky": "^8.0.0",
    "prettier": "latest",
    "sort-package-json": "^1.57.0",
    "turbo": "latest"
  },
  "packageManager": "npm@8.1.0",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  }
}
