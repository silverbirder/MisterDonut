query myQuery {
  getArticles {
    id
    title
    content
  }
}

subscription donutSubscribe {
  donut {
    name
  }
}

mutation addDonutMutation($input: DonutInput) {
  addDonut(input: $input) {
    id
    name
    price
  }
}

query myDonutsQuery {
  donuts {
    id
    name
    price
  }
}
