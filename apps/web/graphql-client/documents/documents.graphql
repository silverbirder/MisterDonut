query myQuery {
  getArticles {
    id
    title
    content
  }
}

subscription donutSubscribe {
  donut {
    name
  }
}

mutation addDonutMutation {
  addDonut(input: { name: "ee", price: 2002, description: "hogehoge" })
}
