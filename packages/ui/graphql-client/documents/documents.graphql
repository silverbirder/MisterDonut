query donuts($name: String) {
  donuts(name: $name) {
    id
    name
    price
  }
}

query donut($id: Int) {
  donut(id: $id) {
    id
    name
    price
  }
}

mutation editDonut($id: Int, $donutInput: DonutInput) {
  editDonut(id: $id, input: $donutInput) {
    id
    name
    price
  }
}

mutation deleteDonut($id: Int) {
  deleteDonut(id: $id) {
    id
    name
    price
  }
}

mutation addDonut($input: DonutInput) {
  addDonut(input: $input) {
    id
    name
    price
  }
}
